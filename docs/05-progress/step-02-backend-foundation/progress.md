# Step 02 - Backend Foundation

Status: Done

## Goal

Create the base Express TypeScript backend.

## Scope

- Initialize `server/`.
- Install backend dependencies with pnpm.
- Add TypeScript config.
- Add Express app.
- Add MongoDB connection.
- Add health check endpoint.
- Add error middleware.

## Planned Deliverables

- Running backend dev server
- Connected MongoDB
- `GET /health`

## Files Changed

- Added `server/package.json`
- Added `server/pnpm-lock.yaml`
- Added `server/tsconfig.json`
- Added `server/src/app.ts`
- Added `server/src/server.ts`
- Added `server/src/config/env.ts`
- Added `server/src/config/database.ts`
- Added `server/src/middlewares/error.middleware.ts`
- Added `server/src/routes/health.routes.ts`

## Verification

- `pnpm typecheck` passed.
- `pnpm build` passed.
- `GET /health` returned `200` from compiled Express app.
- Local MongoDB port `127.0.0.1:27017` is open.
- Mongoose connected to local `eventhub` database and reported `status: connected`.
- `GET /health` returned database status `connected` after Mongoose connection.

## Notes

- MongoDB can use local connection or MongoDB Atlas.
- Environment secrets should stay outside Git.
- The backend currently uses local MongoDB from `server/.env.example`.
- No real `.env` file was created or committed.
- `dist/` output is generated by build and ignored by Git.

## Next Action

Start Step 03 Authentication: user model, register/login APIs, JWT utility, auth middleware, and Flutter auth integration planning.
